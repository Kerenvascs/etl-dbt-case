pip install dbt-postgres
dbt --version #1.2.0 - Up to date!

# dbt init dbt?project
# config profile yml to sync with dbt_project yml according to the adapter, in our case is postgres
# then run dbt_debug to